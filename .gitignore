node_modules/
dist/
shots/
.slidev/
*.log
nohup.out

# 開発機は npm、srgpu は pnpm で入れている。バージョンを固定する正本は
# package-lock.json の方なので、pnpm 側のロックは追跡しない
# (両方を追跡すると、片方だけ更新されて静かにズレる)
pnpm-lock.yaml

# 書き出した PDF / PNG。中身は slides.md から作り直せる
slides-export.pdf
slides-export/
