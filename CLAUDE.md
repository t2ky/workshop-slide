# workshop-slide — Claude Code 向けの手引き

未来構想キャンプ2026 フレイル班の**オンボーディング用 Slidev**。
高校生に、初日の午後までに「自分が何を作るのか」と「どう始めるのか」を
渡すためのスライド。

作る対象そのもの (体験システム) は別リポジトリ `~/latentlink` にある。
**このリポジトリはスライドだけ。** 体験システムの仕様を書き換えたくなったら
それは latentlink 側の話。

---

## 対象読者を間違えない

参加するのは**高校生**で、**プログラミングが初めての人から中級者まで**混ざっている。

- 初めての人が「今日の夜までに動いた」に届くことを最優先にする
- 中級者が退屈しないよう、伸ばせる方向 (カメラ・ビルド構成) は示す。
  ただし**必須には見せない**。初心者が「そこまでやらないと駄目なのか」と
  受け取ると手が止まる
- 用語を出すときは、その場で1行で言い換える。`postMessage` や `iframe` は
  **出さない**。彼らが知る必要がある約束事は `manifest.json` と
  `Frailty.done()` の2つだけ

---

## スライドを書くときの絶対条件

### 1. 必ずブラウザでレンダリングして確認する

**Slidev は枠に収まらない中身を黙ってスクロール要素にする。**
Markdown を見ただけでは気づけないし、はみ出した部分は本番で誰も見ない
(プレゼン中にスクロールする人はいない)。**書いたら必ず確認すること。**

```bash
npm run check          # 全ページをブラウザで開いて、はみ出しを機械的に検出
npm run shots          # 併せて shots/ に PNG を保存する
npm run check -- 12    # 12ページ目だけ
```

`npm run check` は Chrome (`/usr/bin/google-chrome`) で全ページを実際に開き、
スライドの枠と中身の実寸を比べる。はみ出しがあれば exit 1 で落ちる。
**「たぶん入る」で済ませない。**

数値だけで通しても足りない。**レイアウトを変えたら `npm run shots` を叩いて
PNG を目で見ること。** 収まってはいるが読めない、要素が重なっている、
余白が偏っている、といったものは実寸検査では落ちない。

### 2. はみ出したら、中身を減らす

**文字を小さくして詰めるのは禁止。** 読めなくなるだけで、何も解決していない。
はみ出したときの直し方は、上から順に:

1. その文が本当に要るか疑う。喋れば済むものはスライドに書かない
2. スライドを2枚に割る
3. 図・表・アイコンに置き換える

`npm run check` のエラーメッセージも同じことを言う。従うこと。

### 3. 文章過多のスライドを作らない

**スライドは読ませるものではなく、見せるもの。**
喋る内容をスライドに書き写すと、聞き手は読み始めて話を聞かなくなる。

ただし**このスライドは前半と後半で性格が違う**ので、密度を変える。

| | 章 | 密度 |
|---|---|---|
| **前半（理解）** | 1〜4章。フレイル・研究・システム・ビジョン | 説明文を書いてよい。2〜3行の段落、5〜6項目の箇条書きまで許容 |
| **後半（作業）** | 5章以降。約束事・手を動かす・進み方 | 薄く。手が止まらないことが全て |

どちらでも守ること:

- 1スライド = **1メッセージ**。言いたいことが2つあるなら2枚
- 箇条書きの1項目は **1〜2行**。3行に達したら分けるか `.note` に落とす
- コードは **10行以内**。全部見せずに、その日に触る部分だけ切り出す
- 覚えて帰ってほしい一文は `.punch` を使う。**1スライドに1つまで**
- 数字を主役にしたいときは `.hero` (1スライドに1つ) か `.stat`。
  1つの数のために棒グラフを描かない

**下に余白が残るのは正常。** 埋めるために文章を足さないこと。

### 4. 外から引いたら、必ず出典を書く

図・統計・定義を引いたら、そのスライドに `.cite` で出典を添える。
**引用元を書けないものは載せない。**

- 画像は `public/cite/` に置いて**ローカルから読む**。外部URLを直に参照しない
  (会場の回線に依存させないため。フォントと同じ理由)
- 官公庁の資料 (厚生労働省など) は再利用条件が明示されていて扱いやすい
- 数値は「いつ・誰が・何人に」まで書く。「66%」だけでは根拠にならない

---

## 書き方

- **日本語**。高校生に向けた文体で、敬体・常体を混ぜない
- 実測値や具体的な数字があれば書く (「速い」ではなく「0.28秒」)
- スライド中に `<!-- -->` で発表者ノートを書ける。**喋る内容はそこに書く。**
  スライド本体に書かない
- まだ埋まっていない箇所は `<!-- TODO: ... -->` で残す。
  それらしいダミーで埋めて、あるように見せない

---

## 構成

```
workshop-slide/
├── slides.md                 本体。章ごとに `layout: section` の扉を挟む
├── style.css                 全スライド共通の見た目。.punch / .note / .chip / .hero / .screen
├── public/cite/              引用した図。外部URLではなくここから読む
├── scripts/check-overflow.mjs はみ出し検査
└── shots/                    npm run shots の出力 (git 管理外)
```

### スライドを大きく書き換えたら、開発サーバを畳んでから確認する

Slidev の HMR は**スライドの枚数が大きく変わる書き換えに追従しきれない**。
古い中身が途中のページに残ったまま、ページ数だけ新しい、という状態になる。
`npm run check` は通ってしまうので気づけない。

書き換えたあと表示がおかしいと思ったら:

```bash
pkill -f "[s]lidev"                    # 自分のシェルを巻き込まないよう [] で囲む
rm -rf node_modules/.vite .slidev
npm run shots
```

**ページ番号と中身がずれていないかは、`/overview` か `shots/` の連番で確かめる。**

### フォントはネットワークから取らない

Slidev の `fonts:` ヘッダは Google Fonts を取りに行く。**使わないこと。**
会場の回線が読めないので、OS に入っている `Noto Sans CJK JP` を
`style.css` で直接指定している。

ブラウザも同じ理由で、Playwright ではなくシステムの `google-chrome` を
`puppeteer-core` から叩いている。

---

## 元ネタの場所

スライドの内容は、次の3つと食い違わせないこと。**迷ったらこちらが正本。**

| | |
|---|---|
| `~/latentlink/exercises/README.md` | 高校生が守る約束事。**部位の表と `done()` の仕様はここが正本** |
| `~/latentlink/exercises/sample/index.html` | 見本ページ。スライドのコード例はここから引く |
| `~/latentlink/README.md` | 体験全体の設計と、そう決めた理由 |

`~/latentlink/CLAUDE.md` には「予防体験のページは高校生が書く」節があり、
本体側の歯止め (`sanitize_result` / `clamp_knobs`) について書かれている。
**「何を書いても壊れない」とスライドで言い切れる根拠はそこ。**

配布されたスケジュール表 (3日間の時間割) とも突き合わせること。
スライドに書いた時刻がスケジュール表とずれていると、当日そこで止まる。
