# playwright-chromium の postinstall は「バンドル版ブラウザを落とす」処理。
# システムの google-chrome を使うので、意図的に走らせていない
# (インストールは PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 で入れてある)。
#
# pnpm 11 は「ビルドスクリプトを飛ばした」だけでエラー終了するため、
# 警告どまりにする。npm はこのキーを知らないので黙って無視する。
strict-dep-builds=false
